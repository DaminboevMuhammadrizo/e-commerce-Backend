generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

enum UserRole {
  SELLER
  BUYER
  ADMIN
  SUPERADMIN
}

enum ListingType {
  ForSale
  ForRent
}

model User {
  id        Int      @id @default(autoincrement())
  firstName String
  lastName  String
  email     String   @unique
  role      UserRole @default(BUYER)
  password  String
  avatar    String?
  createdAt DateTime @default(now())

  accommodations Accomodation[]
  likes          Like[]
  ratings        Rating[]
  contacts       Contact[]
}

model Accomodation {
  id             Int         @id @default(autoincrement())
  isActive       Boolean
  title          String
  beds           Int
  baths          Int
  garage         Int
  sq_ft          Int
  price          Float
  discount       Float?
  build_year     DateTime
  description    String
  documents      Json?
  map_url        String?
  latitude       Float
  longitude      Float
  country        String
  extra_features Json?
  listing_type   ListingType
  introeVideo    String?
  user_id        Int
  category_id    Int

  accomodationImgs AccomodationImg[]
  user             User              @relation(fields: [user_id], references: [id])
  category         Category          @relation(fields: [category_id], references: [id])
  likes            Like[]
  ratings          Rating[]
  contacts         Contact[]
}

model AccomodationImg {
  id             Int          @id @default(autoincrement())
  url            String
  accomodationId Int
  accomodation   Accomodation @relation(fields: [accomodationId], references: [id])
}

model Category {
  id            Int            @id @default(autoincrement())
  name          String         @unique
  img           String
  icon_img      String
  accomodations Accomodation[]
}

model Rating {
  id               Int   @id @default(autoincrement())
  clean            Float
  location         Float
  communicate      Float
  user_id          Int
  accommodation_id Int

  user          User         @relation(fields: [user_id], references: [id])
  accommodation Accomodation @relation(fields: [accommodation_id], references: [id])
}

model Contact {
  id               Int      @id @default(autoincrement())
  date             DateTime
  time             String
  name             String
  phone            String
  email            String
  message          String
  user_id          Int
  accommodation_id Int

  user          User         @relation(fields: [user_id], references: [id])
  accommodation Accomodation @relation(fields: [accommodation_id], references: [id])
}

model Like {
  id               Int     @id @default(autoincrement())
  like             Boolean
  user_id          Int
  accommodation_id Int

  user          User         @relation(fields: [user_id], references: [id])
  accommodation Accomodation @relation(fields: [accommodation_id], references: [id])
}
